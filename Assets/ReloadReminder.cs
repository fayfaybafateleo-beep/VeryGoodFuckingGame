using UnityEngine;

public class ReloadReminder : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
 public void EndGameobject()
    {
        this.gameObject.SetActive(false);
    }
}
