using UnityEngine;

public class ObjectThoughness : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public int Thoughness;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
